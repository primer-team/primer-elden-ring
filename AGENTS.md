# Project Notes

This project uses Next.js (App Router) + React 19. Use Next.js conventions for entrypoints (`src/app/layout.tsx`, `src/app/page.tsx`), env vars (`NEXT_PUBLIC_*`), and build output (`.next/`).
